<%= form_with(model: countory) do |form| %>
  <% if countory.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(countory.errors.count, "error") %> prohibited this countory from being saved:</h2>

      <ul>
        <% countory.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label "国名", style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit "国名作成" %>
  </div>
<% end %>
